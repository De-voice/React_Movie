import React from 'react'

function NotFound() {
    return (
        <div>
            <h1 className="text-black">the page you are looking for doesn't exist</h1>
            {console.log("not found")}
        </div>
    )
}

export default NotFound
